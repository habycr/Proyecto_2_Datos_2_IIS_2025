<!-- Archivo: AdminWindow.xaml -->
<Window x:Class="UI.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="CodeCoach - Administración de Problemas" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1e1e1e">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ========== PANEL IZQUIERDO: Lista de Problemas ========== -->
        <Border Grid.Column="0" 
                BorderBrush="#3c3c3c" 
                BorderThickness="1" 
                CornerRadius="5"
                Background="#252526">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título del panel -->
                <TextBlock Grid.Row="0" 
                           Text="📚 Problemas Existentes" 
                           FontSize="16" 
                           FontWeight="Bold"
                           Foreground="White"
                           Padding="10"
                           Background="#2d2d30"/>

                <!-- Lista de problemas -->
                <ListBox x:Name="ProblemListBox" 
                         Grid.Row="1"
                         Background="Transparent"
                         Foreground="White"
                         BorderThickness="0"
                         Margin="5"
                         SelectionChanged="ProblemListBox_SelectionChanged"
                         DisplayMemberPath="title"/>

                <!-- Botones de acción -->
                <StackPanel Grid.Row="2" 
                            Orientation="Vertical" 
                            Margin="5">
                    <Button x:Name="RefreshButton" 
                            Content="🔄 Recargar Lista"
                            Click="RefreshButton_Click"
                            Padding="10,8"
                            Margin="0,0,0,5"
                            Background="#0e639c"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"/>

                    <Button x:Name="NewButton" 
                            Content="➕ Nuevo Problema"
                            Click="NewButton_Click"
                            Padding="10,8"
                            Margin="0,0,0,5"
                            Background="#0e7a0d"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"/>

                    <Button x:Name="DeleteButton" 
                            Content="🗑️ Eliminar Seleccionado"
                            Click="DeleteButton_Click"
                            Padding="10,8"
                            Background="#a80000"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Separador -->
        <GridSplitter Grid.Column="1" 
                      Width="10" 
                      HorizontalAlignment="Stretch" 
                      Background="Transparent"/>

        <!-- ========== PANEL DERECHO: Formulario de Edición ========== -->
        <Border Grid.Column="2" 
                BorderBrush="#3c3c3c" 
                BorderThickness="1" 
                CornerRadius="5"
                Background="#252526">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="15">

                    <!-- Título del formulario -->
                    <TextBlock x:Name="FormTitle"
                               Text="📝 Detalles del Problema" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,20"/>

                    <!-- Problem ID -->
                    <TextBlock Text="Problem ID (identificador único):" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="ProblemIdTextBox"
                             Padding="8"
                             Background="#3c3c3c"
                             Foreground="White"
                             BorderBrush="#555"
                             Margin="0,0,0,15"/>

                    <!-- Título -->
                    <TextBlock Text="Título:" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="TitleTextBox"
                             Padding="8"
                             Background="#3c3c3c"
                             Foreground="White"
                             BorderBrush="#555"
                             Margin="0,0,0,15"/>

                    <!-- Descripción -->
                    <TextBlock Text="Descripción:" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="DescriptionTextBox"
                             Height="120"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Padding="8"
                             Background="#3c3c3c"
                             Foreground="White"
                             BorderBrush="#555"
                             Margin="0,0,0,15"/>

                    <!-- Dificultad -->
                    <TextBlock Text="Dificultad:" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    <ComboBox x:Name="DifficultyComboBox"
                              Padding="8"
                              Background="#3c3c3c"
                              Foreground="Black"
                              BorderBrush="#555"
                              Margin="0,0,0,15">
                        <ComboBoxItem Content="Fácil"/>
                        <ComboBoxItem Content="Medio"/>
                        <ComboBoxItem Content="Difícil"/>
                    </ComboBox>

                    <!-- Tags -->
                    <TextBlock Text="Tags (separados por comas):" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>
                    <TextBox x:Name="TagsTextBox"
                             Padding="8"
                             Background="#3c3c3c"
                             Foreground="White"
                             BorderBrush="#555"
                             Margin="0,0,0,15"
                             ToolTip="Ejemplo: intro, math, arrays"/>

                    <!-- Code Stub -->
                    <TextBlock Text="Code Stub (código inicial):" 
                               Foreground="White" 
                               Margin="0,0,0,5"/>

                    <avalonedit:TextEditor x:Name="CodeStubEditor"
                                           Height="100"
                                           FontFamily="Consolas"
                                           FontSize="14"
                                           Background="#1e1e1e"
                                           Foreground="White"
                                           ShowLineNumbers="True"
                                           VerticalScrollBarVisibility="Auto"
                                           HorizontalScrollBarVisibility="Auto"
                                           Margin="0,0,0,20" />


                    <!-- ===== Sección Test Cases ===== -->
                    <Border BorderBrush="#555" 
                            BorderThickness="1" 
                            Padding="10" 
                            Background="#2d2d30"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="🧪 Casos de Prueba" 
                                       FontSize="14" 
                                       FontWeight="Bold"
                                       Foreground="White" 
                                       Margin="0,0,0,10"/>

                            <!-- Lista de test cases existentes -->
                            <ListBox x:Name="TestCasesListBox"
                                     Height="120"
                                     Background="#1e1e1e"
                                     Foreground="White"
                                     BorderBrush="#555"
                                     Margin="0,0,0,10"/>

                            <!-- Inputs para agregar test case -->
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Input:" 
                                               Foreground="White" 
                                               Margin="0,0,0,5"/>
                                    <TextBox x:Name="TestInputTextBox"
                                             Padding="8"
                                             Background="#3c3c3c"
                                             Foreground="White"
                                             BorderBrush="#555"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Expected Output:" 
                                               Foreground="White" 
                                               Margin="0,0,0,5"/>
                                    <TextBox x:Name="TestOutputTextBox"
                                             Padding="8"
                                             Background="#3c3c3c"
                                             Foreground="White"
                                             BorderBrush="#555"/>
                                </StackPanel>
                            </Grid>

                            <!-- Botones de test cases -->
                            <StackPanel Orientation="Horizontal" 
                                        Margin="0,10,0,0">
                                <Button x:Name="AddTestCaseButton"
                                        Content="➕ Agregar Caso"
                                        Click="AddTestCaseButton_Click"
                                        Padding="10,6"
                                        Margin="0,0,10,0"
                                        Background="#0e7a0d"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"/>

                                <Button x:Name="RemoveTestCaseButton"
                                        Content="❌ Eliminar Seleccionado"
                                        Click="RemoveTestCaseButton_Click"
                                        Padding="10,6"
                                        Background="#a80000"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Botón Guardar (grande y destacado) -->
                    <Button x:Name="SaveButton"
                            Content="💾 GUARDAR PROBLEMA"
                            Click="SaveButton_Click"
                            FontSize="16"
                            FontWeight="Bold"
                            Padding="15,12"
                            Background="#0e639c"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Stretch"/>

                    <!-- Consola de mensajes -->
                    <Border BorderBrush="#555" 
                            BorderThickness="1" 
                            Background="#1e1e1e" 
                            Margin="0,20,0,0"
                            Padding="10">
                        <TextBox x:Name="ConsoleTextBox"
                                 Height="150"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Background="Transparent"
                                 Foreground="#00ff00"
                                 BorderThickness="0"
                                 FontFamily="Consolas"
                                 FontSize="12"/>
                    </Border>

                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>