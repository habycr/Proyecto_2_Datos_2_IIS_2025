<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="CodeCoach" Width="1200" Height="750"
        Background="#1e1e1e">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior con botón de Modo Administrador -->
        <Border Grid.Row="0" Background="#2d2d30" Padding="10,5">
            <DockPanel>
                <TextBlock Text="CodeCoach"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="16"
                           DockPanel.Dock="Left" />

                <Button x:Name="AdminButton"
                        Content="⚙️ Modo Administrador"
                        Click="AdminButton_Click"
                        Padding="10,6"
                        Background="#0e639c"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Right"
                        Cursor="Hand"
                        DockPanel.Dock="Right"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- PANEL IZQUIERDO: Filtros + Lista -->
            <Grid Grid.Column="0" Background="#252526">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- FILTROS -->
                <Border Grid.Row="0" Background="#2d2d30" Padding="10" Margin="0,0,0,5">
                    <StackPanel>
                        <TextBlock Text="🔍 Filtros" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                        <!-- Filtro por Dificultad -->
                        <TextBlock Text="Dificultad:" Foreground="White" Margin="0,0,0,3"/>
                        <ComboBox x:Name="DifficultyComboBox"
                                  Background="#1e1e1e"
                                  Foreground="Black"
                                  Margin="0,0,0,10"
                                  SelectionChanged="DifficultyComboBox_SelectionChanged">
                            <ComboBoxItem Content="Todas" IsSelected="True"/>
                            <ComboBoxItem Content="Facil"/>
                            <ComboBoxItem Content="Medio"/>
                            <ComboBoxItem Content="Dificil"/>
                        </ComboBox>

                        <!-- Filtro por Tag -->
                        <TextBlock Text="Tag:" Foreground="White" Margin="0,0,0,3"/>
                        <TextBox x:Name="TagTextBox"
                                 Background="#1e1e1e"
                                 Foreground="White"
                                 Margin="0,0,0,5"
                                 ToolTip="Escribe un tag y presiona Enter"/>

                        <Button x:Name="FilterByTagButton"
                                Content="Filtrar por Tag"
                                Click="FilterByTagButton_Click"
                                Background="#0e639c"
                                Foreground="White"
                                BorderThickness="0"
                                Padding="6,4"
                                Cursor="Hand"
                                Margin="0,0,0,10"/>

                        <!-- Botón Problema Aleatorio -->
                        <Button x:Name="RandomProblemButton"
                                Content="🎲 Problema Aleatorio"
                                Click="RandomProblemButton_Click"
                                Background="#008000"
                                Foreground="White"
                                BorderThickness="0"
                                Padding="6,4"
                                Cursor="Hand"
                                Margin="0,0,0,10"/>

                        <!-- Botón Resetear -->
                        <Button x:Name="ResetFiltersButton"
                                Content="🔄 Mostrar Todos"
                                Click="ResetFiltersButton_Click"
                                Background="#555555"
                                Foreground="White"
                                BorderThickness="0"
                                Padding="6,4"
                                Cursor="Hand"/>
                    </StackPanel>
                </Border>

                <!-- LISTA DE PROBLEMAS -->
                <ListBox x:Name="ProblemList"
                         Grid.Row="1"
                         Background="#252526"
                         Foreground="White"
                         BorderThickness="0"
                         SelectionChanged="ProblemList_SelectionChanged" />
            </Grid>

            <!-- PANEL DERECHO: Descripción + Editor -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>

                <TabControl Grid.Row="0"
                            Background="#2d2d30"
                            Foreground="White">
                    <TabItem Header="Descripción">
                        <ScrollViewer>
                            <TextBlock x:Name="DescriptionText"
                                       TextWrapping="Wrap"
                                       Foreground="White"
                                       Margin="10"/>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal"
                                Grid.Row="0"
                                Margin="5">
                        <Button Content="Run"
                                Click="Run_Click"
                                Margin="0,0,8,0"/>
                        <Button Content="Submit"
                                Click="Submit_Click"/>
                    </StackPanel>

                    <avalonedit:TextEditor x:Name="CodeEditor"
                                           Grid.Row="1"
                                           FontFamily="Consolas"
                                           FontSize="14"
                                           SyntaxHighlighting="C++"
                                           Background="#1e1e1e"
                                           Foreground="White"
                                           ShowLineNumbers="True"/>

                    <TabControl Grid.Row="2"
                                Background="#2d2d30"
                                Foreground="White">
                        <TabItem Header="Console">
                            <TextBox x:Name="ConsoleOutput"
                                     Foreground="White"
                                     Background="#1e1e1e"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="False"
                                     IsReadOnly="True"
                                     VerticalScrollBarVisibility="Auto"/>
                        </TabItem>
                        <TabItem Header="Output">
                            <TextBox x:Name="ResultOutput"
                                     Foreground="White"
                                     Background="#1e1e1e"
                                     IsReadOnly="True"
                                     VerticalScrollBarVisibility="Auto"/>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>